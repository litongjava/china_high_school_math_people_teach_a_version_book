CHAPTER
普通高中课程标准实验教科书数学3
另一种做法是先计算x²的值，然后依次计算x²·x、
（x²·x）·x、（（x²·x）·x）·x的值，这样每次都可以利用
上一次计算的结果.这时，我们一共做了4次乘法运算，
5次加法运算.
第二种做法与第一种做法相比，乘法的运算次数减少
：了，因而能够提高运算效率.对于计算机来说，做一次乘法
运算所用的时间比做一次加法运算要长得多，所以采用第二
种做法，计算机能更快地得到结果
有没有更有效的算法呢？我国南宋时期的数学家秦九韶
（约1202一1261）在他的代表作《数书九章》中提出了下面
的算法.
把一个n次多项式f（x）=ax+a-1x-1+…+ax+ao
改写成如下形式：
f（x）=anr+an-ix-1+..+a)x+ao
=（anr"-l+an-1x-²+·..+a1）x+ao
=(（ar-²+an-1x-3+···+a2)x+a)x+ao
=.·.
=（...（ax+an-1）x+an-2）x+·.+a）x+ao.
求多项式的值时，首先计算最内层括号内一次多项式的值，即
U=ar+dn-1
然后由内向外逐层计算一次多项式的值，即
u=vx+an-2,
=ux+a-3,
...
Un=Un-1x+ao，
这样，求n次多项式f（x）的值就转化为求n个一次多项式
的值
上述方法称为秦九韶算法.直到今天，这种算法仍是多
项式求值比较先进的算法
计算机的一个很重要的特点就是运算速度很快，但即便
如此，算法好坏的一个重要标志仍然是运算的次数.如果一
个算法从理论上需要超出计算机充许范围内的运算次数，那
么这样的算法就只能是一个理论的算法.据说国际象棋一盘
棋的可能下法有10?种，比整个宇宙中的原子还多.因此，
用枚举的办法穷尽国际象棋所有可能下法的算法是永远不可
能实现的
28
