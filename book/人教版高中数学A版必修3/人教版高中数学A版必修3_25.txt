第一章
第一章算法初步
列
用描点法作函数y=x²+3x²-24.xr+30的图象
时，需要求出自变量和函数的一组对应值.编写程序，分别
计算当x=一5，一4.一3，一2、一1，0，1.2.3.4.5
时的函数值
程序：
INPUTX-X
我们看到，用
类BASIC语言编
y=x3+3*x²2-24*x+30
PRINTX
写的计算机程序是
PRINTy
由若干语句行组成
的，计算机按语句
END
行排列的顺序依次
执行程序中的语
在这个程序中，第1行中的INPUT语句就是输入语句
句，最后一行的
这个语句的一般格式是
END语句表示程
序到此结束，
INPUT
“提示内容”；变量
其中，“提示内容”一般是提示用户输人什么样的信息.每
次运行例1中的程序时，依次输人一5，一4，一3，一2.
-1，0，1.2，3，4，5.计算机每次都把新输人的值赋给
变量“x”，并按“x”新获得的值执行下面的语句.
INPUT语句还可以给多个变量赋值.例如，输人一个
①提示内容
学生数学、语文、英语三门课的成绩、可以写成下面的
可以是英文也可以
语句：
是中文
INPUT“Maths.Chinese.EnglishO";a.b,c
你能用输入语句表达1.1.2中程序框图中输入框的内容吗？
例1中第3行和第4行的PRINT语句是输出语句，它
的一般格式是
PRINT
“提示内容”：表达式
输出语句可以在计算机的屏幕上输出常量、变量的值和系统
13
